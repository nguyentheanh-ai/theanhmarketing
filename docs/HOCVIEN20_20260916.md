# Mã giảm giá HOCVIEN20


## 16/09/2026 — HOCVIEN20 cho Codex và Bộ Kit (LOCAL_VERIFIED, chưa phát hành)

Yêu cầu: thêm ô mã giảm giá trên hai form `/academy/codex-x10-hieu-suat` và `/academy/bo-kit-agent-doanh-nghiep`. Mã HOCVIEN20 giảm 20% trên gói agent-kit-offer-990: 990.000đ -> 792.000đ, tiết kiệm 198.000đ. Không đặt hạn sử dụng hoặc kiểm tra danh tính học viên ngoài yêu cầu mã. Nhận chữ thường/khoảng trắng đầu cuối; mã sai chặn gửi, xóa mã quay về giá thường.

Nguồn ứng viên: `/Users/theanh/CodexProjects/Kinh doanh/worktrees/hocvien20-20260916`, branch `feat/hocvien20-20260916`, base canonical `51493e5`. Doctor remote đạt sau khi cho phép kết nối GitHub. Không sửa nguồn release, dữ liệu khách, email, quyền học hoặc webhook.

API `/api/orders` chuyển couponCode đến `createPaymentOrder`; `lib/orders/coupon.ts` kiểm tra đúng mã, đúng product slug, đúng gói990K và một item trước ghi DB/tạo QR. Amount và item price đều792000, mã được ghi trong title để truy vết; payment_plan/slug giữ nguyên để cấp quyền đầy đủ. Không nhận giá do client gửi. Coupon không áp dụng cho gói cọc, phần còn lại, sản phẩm khác hoặc combo. Hợp đồng SePay/fulfillment/tracking dedup giữ nguyên; Lead/InitiateCheckout của form dùng giá sau giảm.

File: client sections Codex; API orders; orderService; coupon helper; loader/static entry và bundle Kit mới index-BXy-GC-t.js; tests hocvien20 coupon/forms, cập nhật alias module trong test support booking. Bản nguồn RegistrationForm.jsx đi kèm tại docs/landing-source/doi-ngu-nhan-su-ai/ để tái dựng/review. Nguồn Vite ứng viên đầy đủ và dependencies dùng lại ở `reports/hocvien20-20260916/landing-source`; chưa đồng bộ lên nguồn Vite gốc trong workspace Hệ thống quảng cáo. Khi phát hành cần đồng bộ đúng RegistrationForm.jsx đã kiểm tra, không ghi đè thay đổi song song. CSS bundle không đổi.

Đã kiểm: 100/100 focused tests gồm hành vi form qua React hook harness, coupon/service với DB/QR giả lập, payment/email/landing/support; TypeScript đạt; lint các file sửa đạt; Vite build đạt; Next webpack production build108 routes đạt sau cấp mạng tải Google Fonts; git diff --check đạt. Không tạo đơn thật, gửi email hoặc replay Purchase. Chưa kiểm trực quan trong trình duyệt vì policy Computer Use/Browser Agent hiện không cho task này; không coi hook harness là browser QA.

Full suite lần đầu793 pass/24 fail/2skip; một lỗi alias mới do test support loader thiếu coupon import đã sửa, test đó và100 tests liên quan hiện đạt. Các lỗi còn lại gồm thiếu môi trường React UI/browser, IPC sandbox và4assertion Facebook Ads (đã tái hiện cả4trên canonical không sửa). Full lint119errors/9451warnings, chủ yếu lint compiled bundles và test lifecycle cũ; scoped lint sạch. Không tuyên bố full suite/full lint đạt.

Bằng chứng: `/Users/theanh/CodexProjects/Kinh doanh/reports/hocvien20-20260916/` với tests.log, build.log, scoped-lint.log, full-tests.log, full-lint.log, baseline-unrelated.log. Đọc registry/rules/policy, ACTIVE_TASKS, protocol/context/state/feature, payment/email, repo AGENTS/current/feature/handoff/design/SePay/database và hướng dẫn use-client. Chờ xác nhận production theo computer-use-policy.json action_time_confirmation_required.production_deploy; sau duyệt tích hợp canonical, preflight exact root và kiểm bản phát hành/route/bundle/runtime. Website thật chưa có mã ở thời điểm bàn giao.


## Đã duyệt phát hành

Anh xác nhận “làm đi” sau bản bàn giao100tests/build đạt. Tiếp tục tích hợp và phát hành; không còn chờ xác nhận production. Rollback hiện tại dpl_43P1wwXLKwqhybb493YWDa7Rg5XX.
